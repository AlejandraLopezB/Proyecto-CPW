<%= form_for(@bloqueada) do |f| %>
  <% if @bloqueada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bloqueada.errors.count, "error") %> prohibited this bloqueada from being saved:</h2>

      <ul>
      <% @bloqueada.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
